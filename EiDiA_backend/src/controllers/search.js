"use strict";


const basicSearch = (req, res) => {
    res.status(200).json({response: "dummy response"});
};

const advancedSearch = (req, res) => {
    res.status(200).json({response: "dummy response"});
};

module.exports = {
    basicSearch,
    advancedSearch,
};
