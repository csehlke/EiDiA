"use strict";

const uploadDocument = (req, res) => {
    res.status(200).json({response: "dummy response"});
};

const addAttributes = (req, res) => {
    res.status(200).json({response: "dummy response"});
};

module.exports = {
    uploadDocument,
    addAttributes,
};
